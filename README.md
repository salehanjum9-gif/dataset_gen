# dataset_gen
Python script to generate synthetic student performance dataset for ML projects. Creates CSV with features: time_taken, attempts, final_result (Pass/Fail). Useful for training classification models when real data is unavailable. Built with Pandas &amp; NumPy.
